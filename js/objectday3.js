const prices = [300,400,100,600,800]

const{} = prices

// console.log(`Rice : ${a}, Beans : ${b}`)

const fruits = ['Mango', 'Apple', 'Pears', 'Lemon']

const[first, ...rest] = fruits;

console.log(first)
console.log(rest)

// const secondFruits = [];

const two = [
    a = 5, 
    b = 10
]
let result = JSON.stringify(two)

console.log(result)
const [num1 , num2] = two;

console.log(` a : ${num2}, b : ${num1}`)
